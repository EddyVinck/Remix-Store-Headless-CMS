export default function CategoriesIndex() {
  // We could return some different UI for the index route here, but we don't need to.
  // Still, the layout route app/routes/categories.tsx will utilize the form on this index and dynamic routes.
  // On the search page, we show the list of categories instead on the index, as opposed to the search results on dynamic pages (search/$searchString.tsx).
  return null;
}
