import * as prismic from "@prismicio/client";

export const repoName = "remix-headless"; // Fill in your repository name.
export const accessToken = ""; // If your repo is private, add an access token.
export const endpoint = prismic.getEndpoint(repoName); // Format your endpoint.
